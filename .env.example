# Agentic-KB environment — copy to .env (gitignored)

# ── Required ────────────────────────────────────────────────────────────────

# Obsidian Vault root (where Sofie writes back).
# Default: ~/Documents/Obsidian Vault
OBSIDIAN_VAULT_ROOT=/Users/jaywest/Documents/Obsidian Vault

# Anthropic API key — required for kb query, kb compile (RLM pipeline)
ANTHROPIC_API_KEY=sk-ant-...

# Claude model used by the web API routes (query/compile/lint/process).
# Defaults to claude-sonnet-5 when unset (see web/src/lib/model.ts).
KB_MODEL=claude-sonnet-5

# ── Web app ─────────────────────────────────────────────────────────────────

# Web server port (default 3002)
KB_PORT=3002

# Base URL exposed to MCP / CLI when calling /api/* endpoints
KB_API_URL=http://localhost:3002

# Deadline (ms) for the CLI/MCP to receive response headers from KB_API_URL.
# Default 30000. Streaming (SSE) bodies are not bounded by this; /api/lint gets
# at least 300000 because it answers only after its Claude call returns.
KB_API_TIMEOUT_MS=30000

# PIN for accessing private wiki content (wiki/personal/**) via web/MCP/CLI
PRIVATE_PIN=

# ── Cost governance ─────────────────────────────────────────────────────────

# Hard cap on Claude API spend per day (USD). kb query refuses past cap.
KB_DAILY_COST_CAP_USD=5

# ── Optional ────────────────────────────────────────────────────────────────

# Suppress first-run env check after initial pass
KB_FIRST_RUN_OK=

# Override KB root (defaults to repo root resolved from cli/kb.js)
KB_ROOT=

# PIN for scripts/pin.mjs lock/unlock/read (avoids the interactive prompt)
AGENTIC_KB_PIN=

# GitHub PAT used by repo sync (MCP sync_repo_markdown) when no token is passed
GITHUB_PAT=

# Sofie Obsidian watcher notifications (scripts/sofie-watch-obsidian.mjs).
# Set the Telegram pair for pushes, or a webhook URL — both optional.
SOFIE_TELEGRAM_BOT_TOKEN=
SOFIE_TELEGRAM_CHAT_ID=
SOFIE_WEBHOOK_URL=
