# Supply-chain guard (Aug 2026 npm worm: keyv/cacheable/flat-cache compromises).
# Lifecycle scripts do not run on install. Override per-invocation with
#   npm install --ignore-scripts=false
# if a dependency legitimately needs a build step.
ignore-scripts=true
