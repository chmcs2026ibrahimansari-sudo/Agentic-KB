---
date: 2026-08-21
type: daily
tags: [daily]
---

# Friday, August 21, 2026

## Today's Priority
> Pull from: [[personal/hermes-operating-context]] → Priority Stack

**P1 (set by 2026-08-20 EOD):** Fix the wiki orphan-generation bug at compile time. **Not done** — orphans moved 73 → 72 on +7 pages, which is noise. Carried; now explicitly gating PROP-157.

**Do-first (set by 2026-08-20 EOD):** Commit `sellerfi` (121 dirty files, "~5 min"). **Not done** — second consecutive day, still 121 dirty, zero commits.

---

## Sessions

_(no manual session entries — note created by eod-wrap-up)_

---

## Decisions Log

| Decision | Rationale | Reversible? |
|----------|-----------|-------------|
| Label the compile gate's `promote` count **ADVISORY** rather than wiring it up | PROP-157: there is no promote generator to connect, and building one before the orphan defect is fixed would multiply orphan creation by 28. Labeling stops the misdiagnosis without pretending to fix it. | Yes |
| Downgrade the whole nightly CI run to **report-only** | Sandbox provisioning failed; the fallback shell was the operator's workstation, not a disposable sandbox — different git identity, different credentials, non-disposable filesystem. Per §5, downgrade rather than act when uncertain. | Yes |
| Do **not** auto-fix the dead `VERCEL_TOKEN` | §5 Tier B places "anything requiring a new secret" out of auto-fix scope. Credential rotation is Jay's. | Yes |
| **2026-08-22 P1 reassigned** from the orphan bug to merging the unmerged nightly branches | Dependency runs one way: `nightly/2026-08-20-improvements` already modifies `promotion.mjs`, a page-creating path the orphan fix must touch. Merging unblocks the orphan fix; the reverse is not true. | Yes |

---

## Open Threads

- **Two unmerged `nightly/*-improvements` branches** — 08-20 (five verified fixes incl. a webhook auth bypass) and 08-21. The 08-20 branch blocks the top two items in `docs/NIGHTLY-BACKLOG.md`; today's nightly run skipped both because of it. One branch added per night, zero merged.
- `sellerfi` — **121 uncommitted files**, second consecutive day, no commits.
- `sellerfi` — `VERCEL_TOKEN` dead (HTTP 403 `invalidToken`). SellerFi#186 is stale; the check has read no Vercel state since the token died.
- `Agentic-Pi-Harness` — `docs/AGENTIC-OPERATING-MODEL-CROSSWALK.md` untracked, **fourth** consecutive EOD carry.
- Nightly sandbox provisioning failed again (`useradd` exit 12), second straight day, 4 retries. §8's abort protocol was fully silent on 08-20 — it runs inside the sandbox it is meant to report the death of.
- `GH_TOKEN` in the nightly task file is still an unrotated plaintext `gho_` user token with full account scope, read into model context every run.

---

## KB Updates

- Daily lint: 770 pages, 0 contradictions, 72 orphans, 307 stale. Credits restored — first clean (non-SKIPPED) lint in three days.
- PROP-157 filed: `gate:decision.promote-has-no-consumer` — 17 consecutive runs reporting `promote: 28-30, graduate: 0` with no page ever created.
- Nightly CI analysis filed at `wiki/reports/2026-08-21-nightly-ci-analysis.md`.
- Two `NIGHTLY-BACKLOG.md` items closed; one new top item added (merge the unmerged branch).
- morning-review apply pass: telephone-game / per-claim-confidence synthesis drafted, PROP-155/156 filed.

---

## 🌙 EOD Wrap-up (18:00)

## 1. WHAT SHIPPED

- **Agentic-KB — found that `promote` has never done anything (PROP-157).** `compile-2source-gate.mjs` computes `decision.promote`, prints it, then shells to `kb compile`, which never receives the list. `wiki/_meta/compile-log.md` shows **17 consecutive runs at `promote: 28–30, graduate: 0`**. Not one page has ever been created from a promote decision. There is no generator to wire up — it was never built. Interim fix landed: plan output and compile log now label the count **ADVISORY** so it stops reading as applied work.
- **Agentic-KB — preflight no longer returns `degraded` for a dirty worktree**, and gained a KB-web-server check. Yesterday's preflight was flagging a permanent condition (`sellerfi` has been dirty for two days) as a run-blocking one.
- **Agentic-KB — compile now points at the launchd server** instead of the dead path that produced the write-phase blockage.
- **Nightly run — two defects found and fixed in one run.** `archiveMove` in `retention.mjs` silently destroyed an archived record when a `task_id` was reused (a plain `copyFileSync` into a flat namespace, in the module whose contract is "archive, never delete"). `resolveRepoWikiDir` returned caller casing on APFS/NTFS, making its canonicalising fallback dead code on every Mac checkout — 512 pass / 1 fail on `main` at clone time. Both on `nightly/2026-08-21-improvements`.
- **MissionControl — PR #126 merged**, Remote Sandbox Hardening V1 blocker remediation. The only merged PR today, and the only repo other than Agentic-KB with any activity.
- **Daily lint: 770 pages, 0 contradictions, 72 orphans, 307 stale.** Credits are working again; lint ran clean rather than SKIPPED for the first time in three days.

**Uncommitted, and this is the real exposure:**
- `sellerfi` — **121 dirty files, second consecutive day, zero commits.** Yesterday's plan called this "~5 minutes" and put it first. It did not happen.
- `Agentic-Pi-Harness` — `docs/AGENTIC-OPERATING-MODEL-CROSSWALK.md`, **fourth** consecutive EOD carry.

**Yesterday's P1 — fix wiki orphan generation at compile — was not done.** Orphans moved 73 → 72 on +7 pages; that is noise, not a fix. It is now blocking more than it was: PROP-157 names it as the explicit prerequisite for any promote-apply work.

## 2. WHAT'S OPEN (ranked by tomorrow's leverage)

1. **Two unmerged `nightly/*-improvements` branches, and only Jay can clear them.** `nightly/2026-08-20-improvements` carries five *verified* fixes — a webhook `X-KB-Namespace` **auth bypass**, a crafted filename injecting frontmatter keys on ingest, an unbounded promotion target, duplicate frontmatter on BOM-prefixed pages, and lock theft from a live holder. It touches `frontmatter.mjs`, `promotion.mjs`, `locks.mjs`, `cli/kb.js` and the ingest webhook — exactly the files the top two backlog items must rewrite. Today's nightly run **skipped both of those items for that reason**. `nightly/2026-08-21-improvements` now sits behind it. A branch is being added per night and none are merging.
2. **`sellerfi` VERCEL_TOKEN is dead** — HTTP 403 `invalidToken` on the scheduled env check. Worse than a broken check: the workflow `exit 1`s before its `gh issue create` block, so a dead token silently suppresses the env-var reporting it exists to produce. Open issue SellerFi#186 is **stale** — nothing has read Vercel state since the token died. One `gh secret set`.
3. **`sellerfi`: 121 uncommitted files, day two.** Loss-bearing, growing, and a direct daily-commit-rule violation.
4. **Wiki orphan generation at compile.** 72 orphans. Unchanged as a defect, but now also gates PROP-157 — adding a promote-apply path before it is fixed multiplies orphan creation by 28.
5. **Nightly sandbox provisioning failed again** (`useradd` exit 12 on `/sessions`), second straight day, four identical retries. §8's abort protocol was **fully silent on 08-20** — no issue, no report — because it runs inside the sandbox whose death it is meant to report.

## 3. TOMORROW'S ONE THING

**Merge or close `nightly/2026-08-20-improvements`, then `nightly/2026-08-21-improvements`.**

This is not the orphan bug, and the swap needs justifying rather than assuming. The orphan bug carried yesterday and did not move — normally that is the signal to re-assert it, not to re-rank. The reason to re-rank here is structural, not a matter of what felt urgent: the unmerged branch already modifies `promotion.mjs`, which is a page-creating path the orphan fix has to touch. Fixing orphans from `main` tomorrow either duplicates that branch or produces a merge conflict resolved by hand. The dependency runs one way — merging unblocks the orphan fix, the orphan fix does not unblock merging.

It is also the only open item that **nothing else can advance**. Every other item on this list is agent-actionable; this one requires Jay's judgment on five security fixes. A queue that grows by one branch per night and drains at zero is not a backlog, it is a leak — and it currently holds an unshipped auth bypass.

**Do two ~5-minute things first, both non-priority:** `gh secret set VERCEL_TOKEN --repo jaydubya818/SellerFi` (the CI check has been reporting nothing, not reporting clean), and commit `sellerfi` — stage by path, not `git add .`.

**Escalation rule, pre-committed:** if the branches are still unmerged at the 2026-08-22 EOD, that is the third day and the item is reported **declined-by-default**, exactly as the credential finding was on 08-20. In that case the branches get closed rather than carried, the five fixes are re-derived on `main` by a future nightly run, and the orphan bug returns to the P1 slot unblocked.

### Open items carrying to tomorrow

- [low/high] Consolidate agent harness research into a structured comparison framework
- [low/high] Document harness engineering patterns as architectural reference
- [low/high] Capture Adobe Meta Factory architecture as reference for pi_agent and hermes design
- [low/medium] Research DeepSeek Harness architecture and compare to current agent harness approach
- [low/medium] Review eval evolution patterns for agent evaluation system design

All five are `recommended`, all low-risk, all KB-capture work. Registry status distribution is unchanged — no finding has transitioned today.

### Shipped today

- [MissionControl] Remote Sandbox Hardening V1 blocker remediation — https://github.com/jaydubya818/MissionControl/pull/126

6 commits, all in Agentic-KB. `morning-review`, `sellerfi` and `Agentic-Pi-Harness`: zero commits.

*Generated by eod-wrap-up at 18:00. Tomorrow's morning-review will pick up from here.*
